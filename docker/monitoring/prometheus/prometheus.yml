global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "classroomhub-backend"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["host.docker.internal:8080"]
    # Nếu chạy trong Docker network thay bằng: ["backend:8080"]
