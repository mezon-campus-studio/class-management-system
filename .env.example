# PostgreSQL credentials — dùng cho docker-compose.yml
POSTGRES_DB=classroomhub
POSTGRES_USER=classroomhub_user
POSTGRES_PASSWORD=classroomhub_pass

# Grafana admin (profile: monitoring)
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=admin
